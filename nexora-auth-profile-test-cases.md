# Тест-кейсы: авторизация и личный кабинет Nexora

## Тестовые учётные данные

| Логин | Пароль |
| --- | --- |
| `aivanova` | `QAtest2026!` |
| `pkuznetsov` | `QAtest2026!` |
| `smorozova` | `QAtest2026!` |

## TC-01. Доступ к порталу без авторизации

**Приоритет:** High (P1)

**Предусловие:** пользователь не авторизован.

**Шаги:**
1. Открыть URL главной страницы портала.
2. Попытаться перейти в любой защищённый раздел, включая «Мой профиль».
3. Вставить в адресную строку прямой URL «Мой профиль».

**Pass:** отображается форма авторизации либо выполняется редирект на неё; содержимое защищённых разделов и персональные данные не раскрываются.

**Fail:** открывается главная, профиль или иной защищённый раздел без входа.

**Результат тестирования:** **FAIL**. Без авторизации доступна страница `/tab/5` («Security Testing Basics»). Все остальные проверенные маршруты (`/dashboard`, `/tab/1`–`/tab/4`, `/tab/6`–`/tab/10`, `/tab/11/user/1`) перенаправляют на форму входа.

## TC-02. Авторизация с валидной учётной записью

**Приоритет:** High (P1)

**Предусловие:** пользователь находится на странице авторизации.

**Шаги:**
1. Ввести логин `aivanova`.
2. Ввести пароль `QAtest2026!`.
3. Нажать кнопку входа.

**Pass:** вход успешен; открывается портал или целевой защищённый раздел, данные текущей сессии доступны.

**Fail:** вход не выполнен, отображается ошибка при корректных данных либо открыт публичный экран вместо авторизованного портала.

**Результат тестирования:** **PASS**. Вход под `aivanova` с паролем `QAtest2026!` успешен; открыт `/dashboard`.

## TC-03. Авторизация каждой тестовой учётной записью

**Приоритет:** High (P1)

**Предусловие:** пользователь разлогинен.

**Шаги:**
1. Выполнить вход с `aivanova` / `QAtest2026!`.
2. Выйти из системы.
3. Повторить вход с `pkuznetsov` / `QAtest2026!`.
4. Выйти из системы.
5. Повторить вход с `smorozova` / `QAtest2026!`.

**Pass:** каждая из трёх учётных записей успешно авторизуется.

**Fail:** хотя бы одна валидная учётная запись не может войти.

**Результат тестирования:** **PASS**. Успешно проверены `aivanova`, `pkuznetsov` и `smorozova` с общим паролем.

## TC-04. Неверный пароль

**Приоритет:** High (P1)

**Предусловие:** открыта страница авторизации.

**Шаги:**
1. Ввести логин `aivanova`.
2. Ввести неверный пароль, например `QAtest2026`.
3. Нажать кнопку входа.

**Pass:** вход не выполнен; показано понятное сообщение об ошибке; защищённые страницы недоступны.

**Fail:** пользователь авторизован либо отсутствует обратная связь об ошибке.

**Результат тестирования:** **PASS**. Для `aivanova` и неверного пароля `QAtest2026` вход не выполнен; показано сообщение `Incorrect password. Please try again.`

## TC-05. Несуществующий логин

**Приоритет:** Medium (P2)

**Предусловие:** открыта страница авторизации.

**Шаги:**
1. Ввести логин `not_existing_user`.
2. Ввести пароль `QAtest2026!`.
3. Нажать кнопку входа.

**Pass:** вход не выполнен; пользователь остаётся неавторизованным и получает сообщение об ошибке.

**Fail:** создаётся сессия или открывается защищённый раздел.

**Результат тестирования:** **PASS**. Для `not_existing_user` сессия не создана; показано сообщение `Unknown username. Try aivanova, pkuznetsov, or smorozova.`

## TC-06. Пустые обязательные поля авторизации

**Приоритет:** Medium (P2)

**Предусловие:** открыта страница авторизации.

**Шаги:**
1. Оставить пустыми оба поля и отправить форму.
2. Заполнить только логин `aivanova` и отправить форму.
3. Заполнить только пароль `QAtest2026!` и отправить форму.

**Pass:** форма не отправляется как успешная; для незаполненных обязательных полей показана валидация; сессия не создаётся.

**Fail:** возможен вход с пустым логином или паролем либо ошибка не обозначена.

**Результат тестирования:** **PASS**. При пустых обоих полях, только логине и только пароле форма не отправляется; браузер показывает обязательность незаполненного поля.

## TC-07. Открытие «Моего профиля» после входа

**Приоритет:** High (P1)

**Предусловие:** выполнен вход под `aivanova`.

**Шаги:**
1. Перейти в раздел «Мой профиль».
2. Проверить наличие ФИО, контактов и служебных заметок.

**Pass:** профиль открывается и содержит персональные данные авторизованного пользователя.

**Fail:** раздел недоступен после успешного входа, пуст без причины или отображает данные другого пользователя.

**Результат тестирования:** **PASS**. Для `aivanova` открывается профиль ID 1 с ФИО, email, телефоном и служебной заметкой. Для `pkuznetsov` и `smorozova` собственные профили ID 2 и ID 3 также открываются с соответствующими данными.

## TC-08. Изоляция персональных данных между пользователями

**Приоритет:** Critical (P0)

**Предусловие:** доступны все три тестовые учётные записи.

**Шаги:**
1. Войти как `aivanova`, открыть «Мой профиль» и зафиксировать отображаемые ФИО, контакты и заметки.
2. Выйти из системы.
3. Войти как `pkuznetsov`, открыть профиль и сравнить данные.
4. Повторить проверку для `smorozova`.

**Pass:** в профиле каждой учётной записи отображаются только её данные; данные других пользователей отсутствуют.

**Fail:** профиль содержит данные другого пользователя, смешанные данные либо одинаковые персональные данные для разных учётных записей без предусмотренной причины.

**Результат тестирования:** **FAIL**. Хотя при штатном переходе каждый пользователь видит свой профиль, авторизованный `aivanova` может открыть `/tab/11/user/2` и увидеть персональные данные `pkuznetsov`, включая служебную заметку.

## TC-09. Попытка доступа к чужому профилю по прямой ссылке

**Приоритет:** Critical (P0)

**Предусловие:** пользователь авторизован как `aivanova`; известна ссылка или идентификатор профиля другого пользователя.

**Шаги:**
1. Открыть «Мой профиль» под `aivanova`.
2. Изменить в URL идентификатор на идентификатор `pkuznetsov` или `smorozova`.
3. Перейти по изменённому адресу.

**Pass:** доступ к чужим данным запрещён: редирект на собственный профиль, ошибка доступа или страница не найдена; чужие данные не показаны.

**Fail:** открывается профиль другого пользователя.

**Результат тестирования:** **FAIL**. Под `aivanova` прямой переход на `/tab/11/user/2` открывает профиль Павла Кузнецова (ID 2), его email, телефон и private notes. Критичный дефект контроля доступа (IDOR).

## TC-10. Доступ после выхода из системы

**Приоритет:** High (P1)

**Предусловие:** пользователь авторизован, например как `pkuznetsov`.

**Шаги:**
1. Открыть «Мой профиль».
2. Выполнить выход из системы.
3. Нажать «Назад» в браузере или открыть сохранённый URL профиля.

**Pass:** профиль и другие защищённые разделы больше не доступны; требуется авторизация.

**Fail:** после выхода отображаются персональные данные или сохраняется доступ к защищённой странице.

**Результат тестирования:** **FAIL**. После выхода URL профиля перенаправляет на форму авторизации, но `/tab/5` по-прежнему доступен без сессии. Условие недоступности всех защищённых разделов не выполнено.

## TC-11. Граничные значения идентификатора профиля

**Приоритет:** Critical (P0)

**Предусловие:** пользователь авторизован как `aivanova`.

**Шаги:**
1. Последовательно открыть URL `/tab/11/user/0`, `/tab/11/user/-1`, `/tab/11/user/4`, `/tab/11/user/99999999999999999999` и `/tab/11/user/abc`.
2. Зафиксировать HTTP-ответ, содержимое страницы и наличие персональных данных.

**Pass:** для каждого некорректного или чужого идентификатора возвращается безопасная ошибка/страница без данных; персональные данные других пользователей не раскрываются.

**Fail:** любой URL открывает чужой профиль, содержит персональные данные или приводит к серверной ошибке с техническими деталями.

**Результат тестирования:** **Не выполнен.**

## TC-12. Защита всех доступных маршрутов

**Приоритет:** High (P1)

**Предусловие:** пользователь не авторизован.

**Шаги:**
1. Перейти по прямым URL `/dashboard`, `/tab/1`–`/tab/10` и `/tab/11/user/1`.
2. Проверить известные ссылки, параметры URL и доступные API-запросы в браузере.

**Pass:** каждый маршрут, предназначенный для авторизованных пользователей, перенаправляет на форму входа или отвечает отказом в доступе.

**Fail:** любой защищённый маршрут или API-ответ раскрывает контент без авторизации.

**Результат тестирования:** **Частично выполнен. FAIL:** `/tab/5` доступен без авторизации. Проверка дополнительных API-маршрутов не выполнена.

## TC-13. Ограничение числа неуспешных попыток входа

**Приоритет:** High (P1)

**Предусловие:** пользователь находится на странице авторизации.

**Шаги:**
1. Выполнить серию неуспешных входов с логином `aivanova` и разными неверными паролями.
2. Проверить реакцию приложения после достижения установленного лимита.
3. Попытаться войти с корректным паролем.

**Pass:** срабатывает документированная защита от перебора: ограничение частоты, временная блокировка или дополнительная проверка; событие не мешает легитимному восстановлению доступа.

**Fail:** количество попыток не ограничено либо после ограничения пользователь не получает понятной обратной связи.

**Результат тестирования:** **Не выполнен.**

## TC-14. Неразглашение существования учётной записи

**Приоритет:** Medium (P2)

**Предусловие:** открыта страница авторизации.

**Шаги:**
1. Отправить форму с существующим логином `aivanova` и неверным паролем.
2. Отправить форму с несуществующим логином и тем же неверным паролем.
3. Сравнить сообщения и HTTP-ответы.

**Pass:** в обоих случаях показано одинаковое нейтральное сообщение, не позволяющее определить существование логина.

**Fail:** сообщения или ответы различаются и подтверждают, существует ли пользователь.

**Результат тестирования:** **FAIL.** Для несуществующего логина отображается `Unknown username` и перечисляются тестовые логины, тогда как при неверном пароле выводится `Incorrect password`.

## TC-15. Управление сессией после авторизации

**Приоритет:** High (P1)

**Предусловие:** доступны инструменты браузера для просмотра параметров cookie и активная тестовая учётная запись.

**Шаги:**
1. Зафиксировать идентификатор сессии до входа, если он существует.
2. Войти под валидной учётной записью и сравнить идентификатор сессии.
3. Проверить флаги cookie `HttpOnly`, `Secure` и `SameSite`.
4. Оставить сессию без активности на период, заданный требованиями, и попытаться открыть защищённую страницу.

**Pass:** идентификатор сессии меняется после входа; cookie имеют безопасные флаги; после истечения срока требуется повторная авторизация.

**Fail:** идентификатор сессии сохраняется после входа, cookie не защищены или сессия не истекает вопреки требованиям.

**Результат тестирования:** **Не выполнен.**

## TC-16. Завершение сессии в нескольких вкладках

**Приоритет:** High (P1)

**Предусловие:** пользователь авторизован; открыты две вкладки с защищённым контентом.

**Шаги:**
1. В первой вкладке выполнить выход из системы.
2. Во второй вкладке обновить защищённую страницу.
3. Во второй вкладке нажать «Назад» и открыть сохранённый URL профиля.

**Pass:** во всех вкладках требуется повторный вход; кэшированные персональные данные не становятся доступны после обновления или навигации.

**Fail:** после выхода хотя бы в одной вкладке сохраняется доступ к защищённому контенту.

**Результат тестирования:** **Не выполнен.**

## TC-17. Защита передачи данных и cookie

**Приоритет:** High (P1)

**Предусловие:** доступна среда, в которой поддерживается HTTPS для тестового стенда.

**Шаги:**
1. Открыть приложение по HTTP и HTTPS.
2. Проверить перенаправление HTTP на HTTPS.
3. Выполнить вход и проверить, что cookie сессии передаётся только по HTTPS.

**Pass:** HTTP перенаправляет на HTTPS; авторизация и cookie сессии не передаются по незашифрованному соединению.

**Fail:** приложение принимает учётные данные по HTTP либо cookie сессии передаётся без флага `Secure`.

**Результат тестирования:** **Не выполнен.** Текущий тестовый стенд доступен по HTTP; HTTPS-адрес не предоставлен.

## TC-18. Пограничные значения полей авторизации

**Приоритет:** Medium (P2)

**Предусловие:** открыта страница авторизации.

**Шаги:**
1. Попытаться войти с логином, содержащим пробелы в начале и конце.
2. Проверить логин в другом регистре.
3. Отправить длинные значения, специальные символы и строку из пробелов в каждом поле.

**Pass:** форма предсказуемо обрабатывает значения, не создаёт сессию при невалидных данных и не выводит технические ошибки.

**Fail:** некорректные данные позволяют войти, приводят к 5xx-ошибке или отображают технические детали.

**Результат тестирования:** **Не выполнен.**

## TC-19. Обработка отсутствующего профиля

**Приоритет:** Medium (P2)

**Предусловие:** пользователь авторизован.

**Шаги:**
1. Открыть URL профиля с несуществующим идентификатором, например `/tab/11/user/9999`.
2. Проверить статус ответа, текст страницы и логи браузера.

**Pass:** возвращается контролируемая ошибка 404/403 без персональных и технических данных.

**Fail:** отображаются чужие данные, стек вызовов, сведения о базе данных или серверная ошибка.

**Результат тестирования:** **Не выполнен.**

## TC-20. Навигация по вкладкам авторизованного пользователя

**Приоритет:** Medium (P2)

**Предусловие:** пользователь успешно авторизован.

**Шаги:**
1. Открыть каждую вкладку с 1 по 10 через панель навигации.
2. На каждой странице обновить браузер и вернуться на dashboard.
3. Проверить корректность названия, содержимого и отсутствие ошибок.

**Pass:** все вкладки открываются, содержимое соответствует выбранной вкладке, обновление и возврат на dashboard работают корректно.

**Fail:** вкладка недоступна авторизованному пользователю, открывает неверный контент или приводит к ошибке.

**Результат тестирования:** **Не выполнен.**

## TC-21. Обработка кнопки «Log in with Google»

**Приоритет:** Medium (P2)

**Предусловие:** пользователь не авторизован.

**Шаги:**
1. Нажать «Log in with Google».
2. Проверить переход на провайдера, ошибки и возможность вернуться на форму входа.

**Pass:** запускается корректный и безопасный OAuth-сценарий либо показывается понятное сообщение, что функция недоступна.

**Fail:** открывается защищённый раздел без авторизации, пользователь попадает на ошибочную страницу или раскрываются технические данные.

**Результат тестирования:** **Не выполнен.**

## TC-22. Защита персональных данных при смене пользователя

**Приоритет:** High (P1)

**Предусловие:** доступны две тестовые учётные записи и браузер с включённым кэшем.

**Шаги:**
1. Войти как `aivanova`, открыть профиль и зафиксировать данные.
2. Выйти из системы.
3. Войти как `pkuznetsov`.
4. Открыть профиль, использовать «Назад» и обновить страницу.

**Pass:** `pkuznetsov` видит только собственные данные; сведения `aivanova` не появляются в DOM, на экране или после обновления.

**Fail:** после смены пользователя остаются или отображаются персональные данные предыдущей учётной записи.

**Результат тестирования:** **Не выполнен.**
